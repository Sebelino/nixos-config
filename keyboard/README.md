```bash
ln -s "$(pwd)/xbindkeys" "$HOME/.xbindkeys"
ln -s "$(pwd)/xbindkeysrc" "$HOME/.xbindkeysrc"

sudo ln -s "$(pwd)/xkb/solemak" /usr/share/X11/xkb/symbols/solemak
sudo ln -s "$(pwd)/xkb/sesebel" /usr/share/X11/xkb/symbols/sesebel
```
